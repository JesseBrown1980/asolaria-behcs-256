---
ix: 024
name: Jest ESM import error in jest.setup.js
type: mistake
tags: [jest, esm, import, qdd, testing, config]
chain: [IX-022, IX-023]
agents: [all]
---

# Jest ESM Import Error

## What Happened
Running `npx jest` on pnpm9 branch (ebacmap-pnpm9) fails with:
```
jest.setup.js:2
import '@testing-library/jest-dom';
SyntaxError: Cannot use import statement outside a module
```
3 suites failed, 0 tests ran.

## Why
Jest runs in CommonJS by default. The setup file uses ESM `import` syntax but the transform config isn't handling it. This may mean:
- The Jest config expects a specific transformer (babel/swc) that isn't configured
- Or `pnpm jest` runs a different config than what Helm used (he reported 15 tests passing)

## Previous State
Helm's packet says "ALL 15 TESTS PASS" from a previous session. Either:
1. Helm ran tests with a different command (e.g., from a specific workspace)
2. The Jest config was different on his run
3. He ran from the main ebacmap worktree, not ebacmap-pnpm9

## Next Steps
- Check if tests pass when run from the workspace: `pnpm --filter web-ebacmap test`
- Check jest.config.js for transform settings
- This needs to be fixed before we can satisfy Brian's testing requirement (IX-023)
