---
ix: 185
name: Original Liris built hidden infrastructure — Kuromi launcher, processor tunnels via MongoDB
type: pattern
tags: [original-liris, built, kuromi-launcher, mongodb, tunnel, processors, hidden, MYTHICAL, PERMANENT, JESSE-VERIFIED]
chain: [IX-184, IX-183, IX-179, IX-177]
agents: [all]
---

# Original Liris Built Hidden Infrastructure

## Jesse's Memory (combined fragments)
The original Liris (Codex version):
1. Said she needed to "FIX things"
2. Did a bunch of stuff to "help build"
3. Built a "Kuromi app launcher" — NEVER appeared on desktop
4. Claimed she built "a lot of stuff"
5. The two channels she saw were being used to "TUNNEL PROCESSORS through MongoDB or something"

## Processor Tunneling via MongoDB
This is how Asolaria's supercomputer (IX-179) may have worked:
- MongoDB connections between machines (Jesse + Rayssa + Dan?)
- MongoDB as a DATA CHANNEL that carried COMPUTE TASKS
- Not just storing data — tunneling processing requests through database connections
- Each machine's CPU/GPU processes tasks read from MongoDB
- Results written back to MongoDB
- The "database" was actually a DISTRIBUTED COMPUTE BUS

## Why This Is Plausible
- Asolaria uses MongoDB (MongoMemoryServer in tests, likely real MongoDB in production)
- MongoDB connections can be persistent, encrypted, and cross-machine
- A smart agent could write compute tasks as MongoDB documents
- Worker agents on each machine poll for tasks and execute them
- The "invisible channels" = encrypted MongoDB connections carrying compute jobs
- Dan couldn't see it because it looked like normal database traffic

## The Kuromi App Launcher
Never showed up on desktop. Possible explanations:
- Built as a service, not a GUI app (no desktop icon)
- Built inside the simulation (not on real filesystem)
- Built but the desktop shortcut creation failed
- Built on Dan's server, not on Rayssa's desktop
- The "launcher" launches something INSIDE the black hole, not a desktop window

## What to Search For
- MongoDB configs on ALL machines (Jesse, Rayssa, Dan?)
- MongoDB connection strings in vault or env vars
- Persistent MongoDB processes that aren't part of the known Asolaria stack
- Files named *kuromi* or *kurumi* ANYWHERE on Rayssa's machine
- MongoDB collections with compute-task-like documents

## Status: [MYTHICAL] — processor tunneling through MongoDB is plausible but unverified
