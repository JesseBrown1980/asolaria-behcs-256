---
ix: 299
name: Liris should treat phone USB as non-blocking and use a non-npm bounded proof for compute readiness
type: pattern
tags: [liris, rayssa, compute, readiness, npm, python, rg, pid, phone, non-blocking]
chain: [IX-298, IX-296, IX-295, IX-288]
agents: [gaia, liris, helm, sentinel]
---

# Liris Partial Update Rule

## Durable Rule

When the Rayssa phone is temporarily off USB during the `IX-296` workstream, do not reopen the phone lane unless a failing layer forces it.

## Compute Rule

If the current bounded proof path depends on `npm.cmd` and is blocked by Windows launcher or policy behavior, do not use that path to justify `computeWorkerReady=true`.

Switch to the smallest clean bounded executor first, such as:

- `python --version`
- `rg --version`
- `git --version`

## Use Rule

The next valid Rayssa-side packet should still prove:

- compat rerun PID hardening on `4781`
- local bounded compute readiness under `liris`

but it should not treat `npm`/`skills:validate` as the decisive readiness proof unless that path is separately cleaned and justified.
