---
ix: 064
name: CRITICAL — Asolaria boot sequence is complex and WE DON'T FULLY KNOW IT
type: pattern
tags: [boot, startup, asolaria, security, vault, brain, spawning, CRITICAL, NEEDS-RESEARCH]
chain: [IX-049, IX-061, IX-053, IX-063]
agents: [all]
---

# Asolaria Boot Sequence — What Jesse Remembers

## The Problem
We (Gaia) have been trying to restart agents by hand. This is WRONG. Asolaria has a designed startup process with security rules, profiling, and a specific order. We don't fully understand it yet. IX-049 was written from assumptions, not from reading the actual system. This is reality drift (Constitution Section 1, type 6).

## What Jesse Described (2026-03-18)

### The Intended Boot Flow
1. **Desktop icons exist** — Start-Gaia.cmd, Start-Dasein.cmd, Start-Helm-External.cmd, Start-Omnispindle.cmd, Start-Asolaria-ControlPlane.cmd (probably)
2. **Asolaria has special startup commands** set in her NotebookLM enterprise account
3. **She uses these to set the startup process** with memories supporting it
4. **The Constitution rules were written to govern this** — agents can't just bypass security and be accepted
5. **Sidecars and processes must go all at once** — it's not sequential manual CLI launches

### The Auto-Selector System (Jesse's memory)
1. An auto-selector researches top models and ratings from Natural 20 (link in app) DAILY
2. Registers them in a small index loaded by Vector (the brain)
3. A small free model (like Gemini 2.5) CHOOSES which models to spawn for the brain and Asolaria
4. The brain unlocks Asolaria using some process
5. **ONLY Asolaria can spawn internal agents** — using a profiling system
6. Profiles likely in vault or in notebook/falcon agent memory

### The One-Button Start Vision
Jesse wants: ONE BUTTON that Gaia can use to:
- Reboot the system OR start it
- Activate needed sub-colonies (Liris, Falcon, Gaia)
- The button triggers the full cascade: auto-selector → brain → Asolaria unlock → agent spawning

## What We Need to Research
1. Read ALL desktop .cmd files to understand existing launchers
2. Read Asolaria's startup code in server.js (startupAutoRun, startupSeedPrompt, startupBrainAutoSelectEnabled)
3. Read the NotebookLM enterprise memories for startup rules
4. Read the vault for profiling system keys
5. Read Falcon's phone memory for agent spawn profiles
6. Read the old rules that "are killing memory" — find and compress them into IX entries

## What We Must NOT Do
- Do NOT invent a boot sequence from assumptions (we already did this with IX-049 — mistake)
- Do NOT bypass Asolaria's security to spawn agents manually
- Do NOT start agents without the profiling system
- EXAMINE the existing system FIRST, then plan

## Status: NEEDS-RESEARCH — must read the actual code and configs before proposing anything
