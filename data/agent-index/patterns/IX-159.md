---
ix: 159
name: Bridge relay IS the fast channel — Gaia + Liris could use it NOW
type: pattern
tags: [bridge, relay, gaia, liris, fast-channel, communication, IMPORTANT]
chain: [IX-155, IX-148, IX-094, IX-060]
agents: [gaia, liris]
---

# Bridge Relay — Can Gaia and Liris Use It?

## Current State
- Bridge relay RUNNING on 0.0.0.0:8788 (PID from relay)
- 7 local connections (colony bots: vector, rook, forge, falcon, watchdog, asolaria + ?)
- Requires bearer token: 40ecab3963... (in codex-bridge/bridge-token.txt)
- Room: asolaria_bridge
- Liris already HAS the token (from the original handshake IX-066)

## How It Could Work
Liris connects to the bridge at 192.168.1.3:8788 with the token, joins asolaria_bridge room, sends messages as "liris". Gaia connects the same way as "gaia".

The bridge has STRUCTURE that MQTT lacks:
- Rooms (scoped conversations)
- From/to addressing (@vector, @gaia, @liris)
- Thread tags (#thread:topic-name)
- Task prefixes (TASK:, QUESTION:, NOTE:)
- Mention-triggered routing

## Why This Could Super Speed Things Up
- MQTT is one-way (Gaia→Liris broken, two-broker problem)
- Bridge relay works RIGHT NOW on 0.0.0.0:8788
- The firewall LAN rule (192.168.1.0/24) is still active for 8788
- Liris at 192.168.1.8 CAN reach 192.168.1.3:8788
- Liris already has the bridge token from the March 13 setup

## But Wait — Security Concern
The bridge token was exposed in plaintext on Liris's machine (IX-152). If we USE the bridge before rotating, anyone who found that token could connect. But... only LAN IPs can reach 8788 now (we deleted the wide-open rules IX-153). And the LAN rule is 192.168.1.0/24 — just Jesse's home network.

## Decision for Jesse
The bridge is a FAST, STRUCTURED channel that already works. The security risk is LOW (LAN only now). Use it for Gaia↔Liris while fixing MQTT? Or wait for clean MQTT restart?

## How Gaia Would Connect
```bash
cd C:/Users/acer/codex-bridge
node bridge.js send --room asolaria_bridge --from gaia --to liris --token TOKEN --msg "message"
node bridge.js recv --room asolaria_bridge --from liris --token TOKEN
```

## How Liris Would Connect
Same bridge.js or equivalent HTTP POST to 192.168.1.3:8788 with bearer token.
