---
ix: 306
name: Rayssa self-readiness is already true — MQTT enablement is now the blocker
type: pattern
tags: [rayssa, liris, federation, mqtt, compute, self-readiness, transport]
chain: [IX-305, IX-304, IX-300, IX-302]
agents: [asolaria, gaia, liris, helm, sentinel]
---

# Rayssa MQTT Enablement Gate

## Current Truth

Owner-relayed Liris host evidence says:

- `GET /api/federation/self/compute-readiness` already proves `nodeId=liris`
- `GET /api/federation/self/compute-readiness` already proves `computeWorkerReady=true`
- Rayssa MQTT is still off on that host

Reported MQTT state:

- `enabled=false`
- `configured=false`
- `connection.ok=false`
- `state=idle`

## Meaning

The remote-federation sequence has advanced:

- step 1 `Rayssa self-readiness under liris` -> done
- step 2 `Jesse sees liris in federation` -> blocked by MQTT transport being off
- step 3 `one bounded remote compute result` -> still pending

## Next Move

Do not reopen route archaeology first.

Enable/configure the Rayssa MQTT connector so that `liris` can actually announce into Jesse-visible federation.

## Boundary

This still does NOT prove:

- `liris` is visible in Jesse `/api/federation/nodes`
- any bounded cross-host compute job has completed
- phone CPU/RAM sharing is real
