---
ix: 307
name: Jesse one-button startup now fails on a real 4781 runtime hang, not startup-memory drift
type: pattern
tags: [jesse, startup, one-button, runtime, health, mqtt, 4781, 4782, hang]
chain: [IX-306, IX-304, IX-303, IX-281]
agents: [gaia, helm, sentinel, liris]
---

# Jesse One-Button Runtime Hang Gate

## Current Truth

The indexed startup command was executed directly:

- `powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\Start-Asolaria-OneButton.ps1 -NoUi`

It failed with:

- `Asolaria failed to become healthy on port 4781. PID=22252`

At the same time:

- Jesse MQTT broker is now correctly bound on `0.0.0.0:18883-18885`
- `4781` is listening on `0.0.0.0`
- direct `GET /api/health/fast` and `GET /api/shared-compute/status` still time out
- raw probe `4782` responds
- `4791` was not reachable in this pass

## Meaning

The startup path is no longer blocked on memory/index confusion.

The current Jesse-local blocker is narrower and more technical:

1. startup command path is known and was followed
2. broker reachability is fixed
3. main runtime enters `server.js` request handling but does not return a response on `4781`

## Boundary

Do not:

- reopen Jesse MQTT identity work
- re-treat stale admin-terminal report windows as primary truth
- blame Rayssa MQTT state for Jesse-local health timeouts

## Next Move

Debug the live main-runtime hang on Jesse:

1. trace `/api/health/fast`, `/api/health`, and `/api/local/ops/status`
2. identify the first await or shared state read that can stall indefinitely
3. preserve the current broker bind fix
4. only retry one-button after the runtime hang is isolated or instrumented

## Coexists With

- `IX-304`: Jesse identity is still live as `jesse-sovereign`
- `IX-306`: Rayssa MQTT transport is still the remote-federation blocker after Jesse runtime is healthy again
