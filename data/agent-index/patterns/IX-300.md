---
ix: 300
name: Rayssa compat rerun is now idempotent and local liris compute preparation is proven, but computeWorkerReady remains false
type: pattern
tags: [rayssa, liris, kuromi, compat4781, rerun, pid, compute, bounded, mqtt, worker-ready]
chain: [IX-299, IX-296, IX-295, IX-288]
agents: [gaia, liris, helm, sentinel]
---

# Rayssa Rerun Compute Boundary

## Durable Rule

Treat the Rayssa-side rerun hardening loop as complete.

- Compat launcher reuse is now keyed to the real listening PID on `4781`
- A second launcher run reuses the same listener PID and does not create a duplicate listener
- `4781` compat health and `4791` gateway health are both good after rerun

## Compute Rule

Treat the current Rayssa-side compute state as preparation, not advertised worker readiness.

- explicit `liris` node identity: proven
- MQTT topic-shape presence: proven
- bounded local execution: proven
- `computeWorkerReady=true`: not proven

The current honest boundary is:

- local bounded compute preparation is real
- advertised worker-contract proof is still missing
- cross-host compute and phone CPU sharing remain unproven from this packet

## Use Rule

Do not reopen the compat rerun bug or the narrow `IX-296` follow-up branch unless new proof contradicts this packet.

For next-phase compute work:

- keep `computeWorkerReady=false` until a real worker contract is advertised and exercised
- do not treat shell-local command proof alone as equivalent to sovereign-dispatched federation proof
