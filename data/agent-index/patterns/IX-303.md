---
ix: 303
name: Jesse sovereign now serves the live shared-compute routes, but federation identity and remote worker proof are still missing
type: pattern
tags: [jesse, shared-compute, rollout, federation, mqtt, node-id, falcon, felipe]
chain: [IX-302, IX-300, IX-297, IX-292]
agents: [asolaria, gaia, helm, sentinel]
---

# Jesse Shared Compute Rollout Boundary

## Durable Rule

Treat the Jesse-side shared-compute route rollout as complete.

- `/api/shared-compute/status` is live
- `/api/shared-compute/workers` is live
- phone workers are visible through the live route surface

## Boundary

Do not treat the current state as full federated compute yet.

- `localWorker.nodeId` is blank
- `localWorker.computeWorkerReady` is false
- MQTT base topic is still `asolaria/test/local`
- federation has zero remote nodes

## Use Rule

Phones can now support the system operationally through the live runtime.

The next compute milestone is not another phone proof.
The next compute milestone is a real federated node proving an advertised worker contract under `asolaria/nodes/<nodeId>`.
