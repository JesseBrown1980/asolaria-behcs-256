---
ix: 304
name: Jesse sovereign MQTT identity is live — computeWorkerReady is true under jesse-sovereign
type: pattern
tags: [jesse, mqtt, identity, federation, compute, baseTopic, jesse-sovereign, gaia]
chain: [IX-303, IX-302, IX-300, IX-297]
agents: [asolaria, gaia, helm, sentinel, liris]
---

# Jesse Sovereign MQTT Identity — Live

## What Changed

Gaia updated the MQTT vault config via `POST /api/integrations/mqtt/config`:

- `baseTopic`: `asolaria/test/local` → `asolaria/nodes/jesse-sovereign`
- Bridge reconnected on the new topic tree automatically
- `nodeId` resolved to `jesse-sovereign` (derived from baseTopic regex in `src/sharedComputeFabric.js:97-100`)
- `computeWorkerReady` flipped to `true`

Timestamp: 2026-03-23 18:26:49 -03:00 (vault `updatedAt`)

## Current Live State

- `localWorker.nodeId` = `jesse-sovereign`
- `localWorker.computeWorkerReady` = `true`
- `baseTopic` = `asolaria/nodes/jesse-sovereign`
- MQTT connection = `ok`, state = `connected`
- shared-compute routes = live
- phone workers visible: `phone:falcon`, `phone:felipe`
- federation remote nodes = 0 (no remote node has announced yet)
- Rayssa phone lane = healthy under `liris/kuromi` (IX-302, re-confirmed by Liris 2026-03-23 ~18:25)

## Boundary

Jesse sovereign is now a real federated compute node with a valid identity.

This does NOT yet prove:

- any remote node is online in federation
- any cross-host compute job has been dispatched or completed
- Amy or Rayssa host is running federated Asolaria

## What Remains Open

1. Jesse phone ADB — zero authorized devices on Jesse side (physical fix needed)
2. Remote federation — at least one node must announce on `asolaria/nodes/<id>` with connected MQTT
3. End-to-end compute proof — a real job dispatched to a remote worker and result returned

## Supersedes

IX-303 boundary section (which said nodeId was blank and computeWorkerReady was false) is now historical.
