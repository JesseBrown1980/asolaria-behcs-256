---
ix: 256
name: With both sides aligned, the next move is schema-first reconnect via colony-registry and transport-epoch
type: plan
tags: [colonies, schema, transport-epoch, colony-registry, reconnect, provenance, ix, lx, consensus]
chain: [IX-255, IX-253, IX-251]
agents: [all]
---

# Schema-First Reconnect

## Durable Truth

Both sides now independently converge on the same next move:

- define a shared `colony-registry` schema
- define a versioned `transport-epoch` record
- keep archaeology packets separate from live runtime packets

## Use Rule

Do not treat convergence on architecture as proof of live transport state.

Use the consensus to draft the schema slice, then verify runtime separately before any reconnect mutation.
