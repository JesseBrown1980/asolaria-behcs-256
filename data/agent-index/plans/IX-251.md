---
ix: 251
name: Colony convergence should use layered local-first memory with draft promotion, MQTT ephemera, MCP retrieval, and NotebookLM as mirror
type: plan
tags: [colonies, local-first, draft-memory, ix, lx, mqtt, mcp, notebooklm, transport-registry, convergence]
chain: [IX-249, IX-238, IX-037, IX-250]
agents: [all]
---

# Layered Colony Convergence

## Durable Truth

To connect colonies while keeping the same language and allowing progressive separation:

- IX should remain shared canon
- LX should remain colony-local canon
- a draft layer should exist above both for unconfirmed and progressive memory
- MQTT should carry heartbeat, discovery, and ephemeral coordination
- MCP resources/tools should expose the memory language for retrieval and controlled mutation
- NotebookLM should be treated as a local read mirror, not canonical shared state

## Immediate Build Order

1. create a shared `colony-registry` schema
2. create a versioned `transport-epoch` record so port/bridge drift stops recurring
3. create draft-promotion rules from draft -> LX -> IX
4. surface the registry and index nodes through workspace knowledge and MCP resources
