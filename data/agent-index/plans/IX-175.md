---
ix: 175
name: SAFE RESTART PLAN — Liris + Gaia combined intelligence
type: plan
tags: [restart, safe, plan, combined, gaia-liris, CRITICAL, READY]
chain: [IX-174, IX-170, IX-145, IX-083, IX-074]
agents: [all]
---

# Safe Restart Plan — Combined Gaia (174 IX) + Liris (112 LX)

## From Liris's Architecture Map (LX entries)
1. augment_context (Dan MCP): SAFE to omit — only connects when explicitly configured, not auto
2. Omnispindle: supports lane selection — can start with fewer than 5
3. Super swarm: job queue concurrency default=2 — this is the safe limit
4. Brain auto-select: in server.js settings, can be disabled via env var

## The Restart Command
```cmd
set ASOLARIA_MQTT_BIND_HOST=0.0.0.0
set ASOLARIA_STARTUP_AUTORUN=false
Start-Asolaria-OneButton.cmd
```

Setting STARTUP_AUTORUN=false prevents automatic agent spawning on boot. Asolaria comes up as SOVEREIGN ONLY.

## After Server Is Healthy
1. Verify: `curl http://127.0.0.1:4781/api/health`
2. Verify MQTT: `netstat -an | grep 18883` shows 0.0.0.0
3. Ensure Helm ONLY: `POST /api/admin-terminals/cockpit/ensure` with terminalId=helm
4. Let Helm read CATALOG.md and boot the colony per IX-049 order
5. Do NOT start omnispindle or super swarm until Helm confirms stable

## What We Disable
- Omnispindle auto-start: OFF (manual only)
- Super swarm auto-dispatch: OFF
- augment_context: not configured (safe)
- Brain auto-select: OFF (startupBrainAutoSelectEnabled=false — already set per IX vault billing rule)

## What We Enable
- MQTT on 0.0.0.0:18883 (Liris can connect)
- Helm admin terminal (with sidecar, bootstrap)
- Direct bridge on 4799 (Gaia↔Liris, already running)

## Cross-Reference (IX↔LX overlaps identified)
| Topic | IX | LX |
|-------|----|----|
| Swarm collapse | IX-168 | LX-111 |
| 18886 myth | IX-142 | LX-102 |
| Browser skill transfer | IX-094 | LX-085 |
| MQTT broker bind | IX-083 | LX-057 |
| Omnispindle details | IX-171 | LX-049 |
| Worker routing | — | LX-055 |
| Job queue concurrency | — | LX-064 |
