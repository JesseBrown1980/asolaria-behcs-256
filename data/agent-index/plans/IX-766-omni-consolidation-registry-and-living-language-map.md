---
ix: IX-766
name: PLAN — Omni consolidation registry and living language map on E
type: plan
tags: [consolidation, runtime, shadow, brown-hilbert, hyperlanguage, 35D, e-drive, ACTIVE]
chain: [IX-473, IX-477, IX-487, IX-650]
pid: asolaria-20260410-omni-consolidation-registry
profile: asolaria
createdBy: codex
agents: asolaria, liris, gaia, all
---

# IX-766 — Omni Consolidation Registry And Living Language Map

## Objective

Centralize truth on `E:` before moving bytes by mapping `normal`, `runtime`, `shadow`, `desktop`, `startup`, and `project` surfaces into one governed registry with 35D bindings.

## Laws

- centralize truth before centralizing bytes
- preserve `normal` and `shadow` as parallel living languages
- do not move or delete files during the registry pass
- use `E:\runtime\omni-consolidation-registry` as the packetized consolidation root

## Required outputs

1. structured surface inventory
2. mirror candidate map
3. review packet
4. LIRIS remote-ingest handoff

## Required next actions

1. review mirror candidates before any byte movement
2. ingest the LIRIS remote root over `192.168.100.2`
3. bind desktop/startup launchers to canonical runtime targets
4. only then materialize selective mirrors from `C:` into `E:`
