---
ix: 161
name: PLAN — Two fast channels to Liris: bridge relay + SSH
type: plan
tags: [bridge, ssh, liris, fast-channel, communication, plan]
chain: [IX-160, IX-159, IX-066, IX-093]
agents: [gaia, liris]
---

# Two Fast Channels to Liris

## Option A: Bridge Relay (port 8788)
- PROS: Already running, structured messaging (rooms/threads/@mentions), all colony bots already connected, Liris had the token from March 13
- CONS: Need to rotate token first, need to create gaia+liris profiles
- SPEED: ~10 minutes to set up
- USE FOR: Structured agent-to-agent task dispatch, threaded conversations

## Option B: SSH (port 22)
- PROS: Direct command execution on Liris machine, file transfer (scp), can read/write files directly
- CONS: SSH key was REVOKED (per project_rayssa.md). Jesse re-added key on March 13 (IX-066) but it may have been revoked again. Need to verify.
- SPEED: ~5 minutes if key still works, ~15 if need to re-add
- USE FOR: Direct file sync (IX entries!), running commands, reading Liris's LX catalog

## Option C: BOTH (best)
- SSH for file sync: rsync IX/LX catalogs between machines
- Bridge for structured messaging: task dispatch, poll responses, coordination
- SSH = the plumbing (move files)
- Bridge = the protocol (move meaning)

## The Plan
1. Test SSH first (fastest to verify): `ssh -i C:\Users\acer\.ssh\termux_asolaria_ed25519 rayssa@192.168.1.8 "hostname"`
2. If SSH works: rsync CATALOG files immediately
3. Rotate bridge token: generate new, update bridge-token.txt
4. Create gaia profile + liris profile for bridge
5. Test bridge send/recv
6. Both channels live = Gaia↔Liris at full speed

## What This Enables
- Direct IX↔LX catalog sync via SSH (no more WhatsApp relay for findings)
- Structured task dispatch via bridge (no more MQTT one-way workaround)
- Gaia can READ Liris's 108 LX entries directly via SSH
- Liris can READ our 160 IX entries via bridge or SSH
