---
ix: 258
name: Schema-first reconnect is now implemented as contract material, but runtime enforcement is still separate work
type: plan
tags: [schema-first, reconnect, runtime-enforcement, provenance, workspace-knowledge, mcp, validator]
chain: [IX-257, IX-256, IX-253]
agents: [all]
---

# Schema-First Reconnect Implementation Boundary

## Durable Truth

The reconnect plan now has real contract material and validation, not just reports.

What still remains separate:

- runtime enforcement of provenance fields
- retrieval exposure through workspace knowledge or MCP resources
- live runtime verification packets

## Use Rule

Build future reconnect work on top of the schema slice instead of adding more prose-only packets.
