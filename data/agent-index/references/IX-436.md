---
ix: IX-436
name: QDD Queue bounded local re-proof rerun reached a partial pass
type: reference
status: active
priority: high
tags: [qdd, queue, reproof, rerun, partial-pass, ftp, mongo, proof-packet, local]
chain: [IX-414, IX-417, IX-424, IX-435]
agents: [gaia, helm, liris, all]
---

The March 29 bounded Queue local rerun is strong enough to count as a credible `partial pass`.

What the rerun proved:

- the local fixture was rebuilt and placed into the FTP drop
- the importer retrieved `ftp-import-tier1-local.csv` from FTP
- the importer moved the file into `imported/ftp-import-tier1-local.csv`
- the real importer/use-case path executed again
- the newest `ftpimportlog` row preserved `5` valid rows and `2` invalid rows
- the first invalid error stayed `Test Definition is required`
- `testresults` did not grow during the rerun window
- `pendingtestresults` did not grow during the rerun window

Observed counts around the rerun:

- before: `ftpimportlogs=6`, `testresults=2294`, `pendingtestresults=6`
- after: `ftpimportlogs=7`, `testresults=2294`, `pendingtestresults=6`

Operational meaning:

- the rerun added a fresh import ledger row
- no new real test results were created in the rerun window
- no duplicate pending growth occurred in the rerun window
- the bounded local lane is therefore stable enough to preserve as a proof packet

Remaining defect:

- the runner still returns `success=false` because FTP-side log upload times out
- exact error from the runner: `Error logging ftp results to server: Data connection timed out.`

Interpretation rule:

- semantic import truth outranks the FTP-side log-upload defect
- this should be treated as a `partial pass`, not as a full failure

Best evidence bundle:

- `D:\projects\QDD\ebacmap-master\reports\qdd-queue-proof-20260329-131203`
