---
ix: 257
name: Colony-memory contract slice now exists locally as schemas, examples, and validation
type: reference
tags: [colony-memory, schema, contract, registry, transport-epoch, draft-promotion, validation, reconnect]
chain: [IX-256, IX-253, IX-242]
agents: [all]
---

# Colony Memory Contract Slice

## Durable Truth

The first machine-readable colony-memory slice now exists under `config/colony-memory/`.

It includes:

- `colony-registry`
- `transport-epoch`
- `draft-promotion-policy`
- example records
- a contract test

## Use Rule

Use these contracts as the canonical schema surface for future colony-memory and transport-truth work, but do not treat them as live runtime facts by themselves.
