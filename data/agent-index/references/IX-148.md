---
ix: 148
name: SECURITY AUDIT — Old bridges, open firewall rules, Oli profiles
type: reference
tags: [security, audit, bridges, firewall, oli, danger, CRITICAL, NEEDS-CLEANUP]
chain: [IX-147, IX-070, IX-065]
agents: [all]
---

# Security Audit — What's Open RIGHT NOW

## Oli — Bridge Companion
Two profiles exist:
- `oli-v1.profile.json` — bridge companion on room `asolaria_bridge`, from `oli`, allowed senders: vector/asolaria/watchdog
- `oli-jarvis-room-v1.profile.json` — Oli in `jarvis_boss_bridge` room, allowed senders: jarvis-boss/jarvis-main/watchdog
- Oli is in `authorityDesktopRegularAllowlist` and `screenViewerAllowlist` in Asolaria settings
- Oli IP was 192.168.1.64

## Firewall Rules — PORT 8788 IS WIDE OPEN
| Rule | RemoteIP | Status | DANGER |
|------|----------|--------|--------|
| Asolaria Relay 8788 (Oli 192.168.1.64) | 192.168.1.64 only | Enabled | LOW — scoped to Oli's old IP |
| codex-bridge-8788-any | **ANY** | Enabled | **HIGH — anyone on any network** |
| codex-bridge-8788 | **ANY** (Private profile) | Enabled | **HIGH — any private network device** |
| Asolaria Relay 8788 (LAN) | 192.168.1.0/24 | Enabled | MEDIUM — whole LAN subnet |
| Asolaria Relay 8788 (Tailscale) | ? | Enabled | MEDIUM — Tailscale network |

## MQTT Firewall Rules
| Rule | Ports | RemoteIP | Status |
|------|-------|----------|--------|
| MQTT Broker LAN | 18883-18886 | 192.168.1.8 only | OK — scoped to Liris |
| Asolaria MQTT Broker (TCP) | 18883 | ? | Needs check |

## SSH Authorized Keys
No authorized_keys file found — SSH key access appears clean (Rayssa's key was revoked per project_rayssa.md)

## Bridge Token
One token: `40ecab3963...` — anyone with this token + access to port 8788 can connect to the relay and communicate with ALL agents.

## CRITICAL FINDINGS
1. **codex-bridge-8788-any** allows ANY IP to connect to the bridge relay. This is a security hole.
2. **codex-bridge-8788** allows any Private network device (not just LAN).
3. The bridge token is the only auth — if it's been shared with Oli/Connor/Brian, they could still connect.
4. Oli has TWO bridge profiles and is in the allowlists.

## Recommendations BEFORE Opening MQTT
1. DELETE or DISABLE `codex-bridge-8788-any` (too broad)
2. DELETE or DISABLE `codex-bridge-8788` (too broad)
3. Keep `Asolaria Relay 8788 (Oli 192.168.1.64)` only if Oli is still active
4. Keep `Asolaria Relay 8788 (LAN)` only if we trust the entire 192.168.1.0/24 subnet
5. Rotate the bridge token if it was shared with anyone who shouldn't have access now
6. Ask Jesse: Is Oli still part of the colony? Is Connor? Is Brian?
