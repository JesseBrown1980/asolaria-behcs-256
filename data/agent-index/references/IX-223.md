---
ix: 223
name: Rayssa friend bridge pack is staged, not merged into live policy or registry
type: reference
tags: [rayssa, bridge, policy, registry, qdd, 4799, inspection]
chain: [IX-221, IX-222, IX-160, IX-159, IX-187]
agents: [all]
---

# Rayssa Friend-Lane Reality

## Findings

- The live airlock room set contains `asolaria_external`, `asolaria_internal`, `asolaria_ops`, `asolaria_bridge`, and `jarvis_boss_bridge`.
- `asolaria-friend-rayss` is absent from the live airlock policy.
- The live sender-token map also omits `rayss-codex`, `gaia`, and `liris`.
- `agents-registry.json` remains anchored on room `asolaria_bridge` with allow agents `asolaria`, `vector`, `rook`, `forge`, `falcon`, and `watchdog`.
- `codex-bridge/data` persists only `asolaria_bridge.json` and `jarvis_boss_bridge.json`.
- The Rayssa friend pack exists under `reports/friend-codex-bridge-packs/...`, but that is staged pack material rather than merged runtime state.
- The pack profile declares `requireRoomPolicy=true`, but that key was not found in the scanned `colony-agent-bot.js`, so enforcement is not code-verified from the sovereign host side.
- Current QDD smoke instructions still tell `Liris` to post results to `4799`, but no listener is live there.

## Meaning

Treat the Rayssa path as prepared but inactive on the sovereign bridge runtime.

For QDD, the practical path remains fallback relay or stale instructions until one bridge lane is deliberately restored and re-documented.
