---
ix: IX-444
name: QDD NovaLUM log forensics show current sync reached the local server and old docs match the dashboard state
type: reference
status: active
priority: critical
tags: [qdd, novalum, logs, sync, dashboard, binary-mode, protocol, forensics]
chain: [IX-442, IX-443]
agents: [gaia, helm, liris, all]
---

The NovaLUM log review is now materially complete.

## What the readable current log proves

Primary readable app log:

- `C:\Users\acer\Downloads\logs\NL2XSync_2026-03-29.log`

Strongest preserved sequence:

- `Sync button clicked - Starting serial-to-socket proxy service...`
- `Preparing for sync operation by refreshing device connection...`
- `Successfully refreshed connection on port COM9`
- `Detection approach 1: Testing if device is already in binary mode`
- `Device is already in binary mode and responding to commands`
- `Connecting to sync socket server at 192.168.1.43:443...`

Two earlier attempts in that same log failed with:

- `Error connecting to sync socket server: ... a máquina de destino as recusou ativamente`
- `Proxy communication failed or was interrupted.`

Meaning:

- the older app log clearly captured the pre-fix state where the host listener was absent
- that earlier failure was a real refused-connection error, not a device discovery error

## What the current server-side log proves

Primary server logs:

- `D:\projects\QDD\ebacmap-master\reports\novalum-sync-server-20260329-140722.out.log`
- `D:\projects\QDD\ebacmap-master\reports\novalum-sync-server-20260329-140722.err.log`

Strongest preserved sequence:

- `STARTING SERIAL SERVER 443`
- `listening on port 443`
- `client connected ::ffff:192.168.1.43:54850`
- `STARTING_SYNC v.2.0.1 []`
- `socket closed ::ffff:192.168.1.43:54850`
- `Error: read ECONNRESET`

Meaning:

- after the host-side fix, the sync no longer dies at connect-time
- the current live boundary is later: the client reaches the server, sync begins, then the peer resets the connection

## What the dashboard wording means

The source tree shows there is no special user-facing “already connected, ignore sync” rejection path.

Relevant current source:

- `D:\projects\QDD\novalink-desktop-sync-app\ViewModels\MainViewModel.cs`
- `D:\projects\QDD\novalink-desktop-sync-app\Services\DeviceService.cs`
- `D:\projects\QDD\novalink-desktop-sync-app\Services\SerialCommunicationService.cs`
- `D:\projects\QDD\novalink-desktop-sync-app\Services\SocketClientService.cs`

Operational meaning:

- pressing `Sync` while the device is already USB-connected is the normal path
- the app deliberately disconnects and refreshes the COM connection before sync
- the most likely “already ...” dashboard line is informational:
  - `Device is already in binary mode and responding to commands`
- that line is not a failure by itself

## What older documentation proves

Older documentation already described this same app behavior.

Primary older document:

- `C:\Users\acer\Asolaria\tmp-qdd-doc.txt`

What it preserves:

- the desktop app continuously monitors for a connected NovaLum device
- it reads the device’s Wi-Fi sync server address and port
- clicking Sync starts communication with the sync server
- the desktop app acts as a socket proxy and encryption middleware between the device and the sync server
- NovaLum device communication is unencrypted proprietary binary protocol
- the app switches the device from terminal mode to binary mode before sync
- detailed log files are expected artifacts

## Older log surface

An older historical NL2X log exists:

- `C:\Users\acer\CrossDevice\Jesse's S24 FE\storage\Download\NL2XSync_2025-06-12.log`

but it is currently not readable because the cloud file provider is not running.

## Practical conclusion

The current live state is now sharply defined:

1. device USB/COM discovery works
2. binary-mode preflight works
3. the app knows the right host and port
4. the laptop now serves the host and port
5. sync reaches `STARTING_SYNC`
6. the connection is then reset during the protocol/proxy phase

So the next blocker is no longer dashboard wording, missing logs, missing host, or missing device config.

The next blocker is inside the live proxy/handshake phase after connection has already been established.
