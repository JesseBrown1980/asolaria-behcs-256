---
ix: IX-397
title: QDD promotion findings 2026-03-27
type: reference
status: active
tags: [qdd, pre-dev, review, ez-protect, queue, promotion]
chain: [IX-395, IX-396]
---

# QDD Promotion Findings — 2026-03-27

Purpose: preserve the current reviewer-grade and promotion-grade findings against the reconstructed split branches so later agents do not have to rediscover them after a crash.

Current review method:

- normal process when the branch hits a wall:
  - `6` reviewers on the obvious/front surfaces
  - `6` reviewers on the supporting/behind-the-surface paths
- fallback when thread limits or urgency prevent the full spread:
  - `3` reviewers on the obvious/front surfaces
  - `3` reviewers on the supporting/behind-the-surface paths
- consolidate only high-signal overlap back into indexed notes instead of relying on one long chat memory
- helper/admin lanes are allowed at pressure points:
  - one bounded helper for note/index hygiene
  - one bounded helper for narrow technical diagnosis
  - helper lanes support the main execution path; they do not replace it

## Queue / SFTP

Current verified state:

- targeted queue timezone/date failures are fixed
- targeted queue tests now pass:
  - `apps/queue-server/src/utils/pendingTestResult.unit.ts`
  - `apps/queue-server/src/use-cases/ImportFtpResults/UseCase.unit.ts`
  - `apps/queue-server/src/use-cases/ImportTestResult/UseCase.unit.ts`
- dead auth bypass hook removed from shared middleware
- root `jest.preset.js` was restored locally in the split worktree

Current gate:

- the old `mongodb-memory-server` MD5 blocker is stale for the current worktree
- `apps/queue-server/src/Queue.unit.ts` now points at a plain local Mongo URI instead of `mongodb-memory-server`
- `Queue.unit.ts` now passes cleanly from the current worktree
- local harness fixes that cleared it:
  - `MongoQueue` loads after the `SendReportPdfUseCase` mock
  - queue worker uses a separate named db connection from the test connection
  - first queue case forces immediate processing
  - remaining queue test timeouts/expectations were aligned to current queue behavior
- `apps/queue-server/src/Queue.unit.ts` remains part of the current local queue change set

Promotion implication:

- queue/SFTP was accepted for `dev`
- queue/SFTP now has a passing focused staging-smoke execution record
- queue/SFTP no longer carries the old queue test blocker; remaining promotion concern is live-environment depth before `prod`

## EZ Protect

Current verified state:

- targeted EZ unit coverage now passes:
  - `apps/web-ebacmap/src/utils/ezProtect.unit.ts`
  - `apps/web-ebacmap/src/modules/ez-protect/repos/EzProtectRepo.unit.ts`
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/ExportEvents/UseCase.unit.ts`
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/GetDeviceHealth/UseCase.unit.ts`
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/GetEzProtectEvent/UseCase.unit.ts`
- first dedicated EZ route integration slice now passes:
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/GetEzProtectDashboard/route.int.ts`
  - verified under `apps/web-ebacmap/jest.ez-protect.integration.config.js`
- second dedicated EZ route integration slice now passes:
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/ListEzProtectEvents/route.int.ts`
  - verified under `apps/web-ebacmap/jest.ez-protect.integration.config.js`
- dead auth bypass hook removed from shared middleware

Current local repair set:

- `apps/web-ebacmap/src/pages/api/ez-protect/events.ts` now has a corresponding local `ListEzProtectEvents/UseCase`
- restored EZ integration support files live under `apps/web-ebacmap/test/`
- restored EZ db model files and wiring live under `libs/db/src/models/` and `libs/db/src/index.ts`
- `apps/web-ebacmap/src/common/core/Endpoint.ts` now hydrates `ezProtect` permissions
- explicit local EZ use-case unit tests now exist:
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/ExportEvents/UseCase.unit.ts`
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/GetDeviceHealth/UseCase.unit.ts`
  - `apps/web-ebacmap/src/modules/ez-protect/use-cases/GetEzProtectEvent/UseCase.unit.ts`

Current gate:

- the old dashboard route blocker is stale
- multiple route-backed and non-route-backed EZ slices are now green
- current targeted EZ evidence set is credible for a `pre-dev -> dev` review pass in this branch shape
- the next EZ no-go reason is now primarily that the current local repair set is still dirty and unformalized

Repair-set clusters to preserve:

- auth / shared middleware
- db wiring / EZ models
- route and unit tests
- integration harness / endpoint plumbing

Promotion implication:

- EZ Protect was accepted for `dev`
- EZ Protect now has a passing focused staging-smoke execution record for the two live route surfaces
- remaining EZ concerns are live-environment depth before `prod` plus non-blocking warnings around `ts-jest` config and duplicate mongoose index definitions

## Staging-Smoke Evidence

Queue / SFTP:

- focused staging-smoke rerun passed
- result:
  - 3 suites
  - 15 tests
- scope:
  - `pendingTestResult.unit.ts`
  - `ImportFtpResults/UseCase.unit.ts`
  - `ImportTestResult/UseCase.unit.ts`

EZ Protect:

- focused staging-smoke rerun passed
- result:
  - 2 suites
  - 7 tests
- scope:
  - `GetEzProtectDashboard/route.int.ts`
  - `ListEzProtectEvents/route.int.ts`
- non-blocking warnings observed:
  - `ts-jest` deprecated config warnings
  - duplicate mongoose index warning on `{ clientId: 1 }`

## Promotion Rule

Promotion order remains:

1. `pre-dev`
2. `dev`
3. `staging`
4. `prod`

Current gate conclusion:

- Queue / SFTP:
  - `dev`: accepted
  - focused `staging` smoke: passed
- EZ Protect:
  - `dev`: accepted
  - focused `staging` smoke: passed
- global `staging` landing is still not claimed in the index
- `prod` remains blocked until each lane has stronger live-environment smoke depth

## Forced Baseline

- see `IX-398`
- shorthand:
  - `QDD = pnpm10`
  - any split branch on `pnpm 9` is drift, not truth

Immediate consequence:

- split-branch pseudo-live or promotion evidence is secondary until `pnpm 10` alignment is restored from source truth
- current minimum alignment set is small:
  - queue split: `package.json`, `bitbucket-pipelines.yml`
  - EZ split: `package.json`
- forward alignment is now applied to those exact files
- queue pseudo-live is now revalidated on the correct `pnpm 10` baseline
- valid queue pseudo-live invocation uses the app-local config:
  - `apps/queue-server/jest.config.js`
- result on valid baseline:
  - 3 suites passed
  - 15 tests passed
- earlier queue pseudo-live failure under `test/unit/jest.config.js` is now treated as harness/config mismatch, not branch logic failure
- additional significance:
  - root test harness drift is now explicitly corrected in both split branches
  - `globals.ts-jest` deprecation cleanup is part of the active repair set
  - duplicate `{ clientId: 1 }` mongoose warning source in `EzProtectMsaConfig` is removed
  - later agents should treat old warning-heavy runs as stale unless rerun after this cleanup
  - the same low-risk cleanup is now mirrored into `ebacmap-master`, so source truth and split repair state are no longer diverging on this issue
- remaining warning note:
  - one duplicate mongoose `{ clientId: 1 }` warning still appears in the queue repo-wide bounded run
  - the current trace routes through `D:\\projects\\QDD\\ebacmap-master\\libs\\db\\src\\models\\EzProtectEvent.ts`
  - this is now treated as a shared source-model investigation item, not split-branch drift

## True Live Smoke Task

Current rule:

- use `true-live-smoke-task-20260327.md` as the environment-facing execution packet
- use `3` front + `3` behind for this phase
- do not widen to `6` + `6` unless the work returns to a branch wall with multiple unclear causes

Why:

- the branch is no longer blocked by broad code ambiguity
- the remaining uncertainty is concentrated in environment inputs and real runtime proof
- this phase needs execution planning and evidence capture, not large-scale rediscovery

## Enterprise Test Sequence

Current testing-order rule:

- generate enough realistic data first
- run pseudo-live smoke second
- build rerouters/adapters third
- rerun through rerouters fourth
- execute true live smoke last

Why this matters:

- realistic data quality determines whether the later smoke stages mean anything
- pseudo-live isolates application/runtime behavior before integration boundaries are added
- rerouters simulate enterprise integration seams without overclaiming real deployment proof
- true live smoke should validate a nearly-stable path, not be used as first discovery

Current minimum realistic datasets are now defined:

- Queue / SFTP:
  - `1` client
  - `1` location
  - `1-2` sites
  - `1-2` test definitions
  - `1-2` scheduled swab/test records
  - `1` import file with `3-10` rows covering normal, missing-date, timezone-edge, and duplicate/failure paths
- EZ Protect:
  - `1` client
  - `1` location
  - authorized and unauthorized user shapes
  - `10-25` events across devices, statuses, and timestamps

Pseudo-live is now the next execution phase after data generation.

## Dataset Tiering

Current dataset rule:

- Tier 1:
  - minimum realistic gate dataset
- Tier 2:
  - bulk/stress follow-up dataset

Known significance:

- a much larger prior dataset exists at roughly `11,000+` sample entries
- that large dataset is useful, but it should be used after the small realistic gate passes
- do not use the giant dataset as the first pseudo-live gate

## Dataset Generation

Current next-step rule:

- build the Tier 1 Queue / SFTP dataset first
- build the Tier 1 EZ Protect dataset second
- then run pseudo-live smoke

The Tier 1 generation tasks are now explicitly written and grounded in the current queue and EZ harness files.

The exact Tier 1 fixture shapes are now also written so later agents do not have to rediscover the accepted payload/event shape from test files.

The next immediate execution layer is now explicit:

- build the Queue / SFTP Tier 1 bundle
- build the EZ Protect Tier 1 bundle
- then start pseudo-live smoke

Current state advance:

- both Tier 1 bundles have now been generated under `tier1-bundles-20260327`
- the next step is no longer fixture design
- the next step is pseudo-live execution using those generated bundles

## Liris pnpm10 chamber

Current significance:

- Liris branch/package on `chore/pnpm-10-upgrade` is now the strongest comparison chamber for pnpm10 provenance.
- The stale local split branches should be treated as reconstructed aids, not source truth.
- Use `IX-399` for bundle-first comparison order and PR interpretation.
