---
ix: 295
name: Fresh Rayssa phone proof bundle resolves the stale smoke contradiction and confirms the liris kuromi lane
type: reference
tags: [rayssa, liris, kuromi, phone, proof, adb, reverse, smoke, compat, host-scope]
chain: [IX-294, IX-293, IX-287, IX-291]
agents: [all]
---

# Fresh Rayssa Phone Proof Bundle

## Durable Truth

A fresh Rayssa-host-only proof bundle now re-proves the phone compat lane in one internally consistent packet:

- `GET /api/mobile/bootstrap` returns `200` on `proxy_4781`
- bootstrap advertises `viewer=liris`
- `GET /api/mobile/session?channel=usb` returns `200`
- `GET /api/mobile/control/status?channel=usb` returns `200`
- `adb devices -l` shows the USB serial and wireless endpoint as authorized
- USB-scoped reverse proof shows `tcp:4781`
- the fresh phone tunnel smoke report passes and agrees with the direct proof

## Boundary

The smoke JSON still includes `hostVisiblePhoneCount: 0`, but that does not override the explicit authorized-device list, selected USB serial, or direct `adb reverse --list` proof unless a later failing probe points at that specific field.

## Use Rule

Treat the stale smoke contradiction preserved in `IX-294` as resolved.

On Rayssa host, the default phone-side label remains `liris/kuromi`, not `falcon`.

Compare future failures only at the failing raw-proof layer; do not reopen the older `/api/mobile/bootstrap = 404` packet unless fresh host-scoped proof forces it.
