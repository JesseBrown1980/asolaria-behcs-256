---
ix: IX-463
name: Jesse-host restart truth is no longer missing source or main runtime; the hard blocker is mixed-format gateway audit logging
type: reference
status: active
priority: high
tags: [jesse, restart, gateway, audit, attribution, 4781, 4791, 5444, sandbox, liris, rayssa, CRITICAL]
chain: [IX-460, IX-309, IX-321, IX-264]
pid: gaia-20260329-jesse-system-restart
profile: gaia
createdBy: gaia
agents: [gaia, all]
---

The current Jesse-host restart packet is narrower than the earlier Amy bootstrap bundle.

## Live host facts

- `C:\Users\acer\Asolaria` is already the real local source tree on this machine
- `node server.js` is already running as the main runtime on PID `17656`
- `http://127.0.0.1:4781/api/health/fast` is healthy
- gateway `src/gateway/server.js` is already running on PID `10288`
- `http://127.0.0.1:4791/health` is healthy at the transport layer
- legacy memory indexer `services/memory-indexer/server.js` is already running on PID `16644`
- sandbox manager is not currently running on this host

## Real blocker

The gateway is not honestly start-ready because audit integrity is locked:

- readiness fails on `audit_integrity_ok`
- readiness fails on `audit_gate_open`
- current gate reason is `entry 1146 prevHash mismatch`

That mismatch is explained by a real local format collision:

- `src/gateway/auditLog.js` expects `data/gateway-audit.ndjson` to contain hash-chained gateway entries with `id`, `type`, `payloadHash`, `prevHash`, and `hash`
- `middleware/attribution.js` appends plain loopback mutation records into the same file without those chain fields
- the first raw attribution entry appears exactly at line `1146`

## Operational boundary

Do not spend time on source restore or new bootstrap packs for Jesse-host restart.
The live work is:

1. stop writing raw attribution events into the hash-chained gateway audit file
2. preserve and rebaseline the mixed audit file with backup and manifest
3. restart only the affected surfaces
4. re-verify `4781`, `4791`, and readiness

## Liris boundary

The local Liris sidecar on Jesse's machine is not the target delivery lane for Rayssa.
For this pass, Liris-on-Rayssa is a post-restart handoff consumer, not a local bootstrap target.
