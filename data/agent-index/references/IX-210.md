---
ix: 210
name: ALL PORTALS FOUND — every way Jesse can talk to Asolaria
type: reference
tags: [portals, super-admin, interfaces, communication, FOUNDATIONAL, PERMANENT]
chain: [IX-209, IX-208, IX-205, IX-181, IX-180]
agents: [all]
---

# Every Portal Into Asolaria — Complete Map

## 1. Desktop App Shell (Advanced Mode)
```powershell
tools/Open-Asolaria-AppShell.ps1 -Mode advanced -Port 4781 -SkipStart
```
Opens Chrome at /?uiMode=advanced&startup=1. Full super admin conversational interface.

## 2. Super Admin Terminal
```powershell
tools/Open-Asolaria-SuperAdminTerminal.ps1 -Target admin -Port 4781
```
Visible local monitor window for the super admin lane. Windows Terminal hosted.

## 3. Direct Codex Window (Super Admin Mode)
In public/index.html — talks directly to internal Codex lane. Bypasses RouteLLM. Avoids external providers. Internal-only via brainOrchestrator.js.

## 4. Admin Terminal Cockpit
In public/index.html dashboard — stacked Helm over Sentinel control surface. Live transcript tails, state pills, prompt boxes, launch/stop/rebrief controls.

## 5. Phone Gemini Edge Lane
/api/phone/edge/gemini with chatId for persistent memory. Records turns to shared memory. Context carries across sessions.

## 6. Phone Interface
```powershell
tools/Start-Asolaria-Phone.ps1 -OpenOnPhone
```

## 7. API Endpoints (loopback = super master)
- POST /api/admin-terminals/cockpit/ensure
- POST /api/admin-terminals/cockpit/prompt
- POST /api/startup/brain-leader/run
- POST /api/asolaria/runtime-actions/run

## 8. Talk-To-Asolaria.ps1
```powershell
Talk-To-Asolaria.ps1 -ChatId main -RouteMode direct
```
DIRECT chat terminal to the Asolaria brain. The conversation channel.

## Jesse's Next Move
The server needs to be RUNNING for any of these to work. Once restarted with anthropic as brain, ALL of these portals become active. Jesse can talk to Asolaria through any of them.
