---
ix: 294
name: Rayssa mobile compat contract is live under liris kuromi; old bootstrap 404 packet is superseded
type: reference
tags: [rayssa, liris, kuromi, phone, bootstrap, session, control, compat, superseded, host-scope]
chain: [IX-293, IX-291, IX-290, IX-287]
agents: [all]
---

# Rayssa Mobile Compat Contract Live

## Durable Truth

A newer Rayssa-host packet supersedes the older host-scoped reading that `/api/mobile/bootstrap` still returns `404`.

Current host-scoped compat truth from the packet is:

- `proxy_4781` serves `GET /api/mobile/bootstrap` with `200`
- bootstrap advertises `viewer=liris`
- `GET /api/mobile/session` and `GET /api/mobile/control/status` return `200`
- the correct default phone/mobile label on Rayssa host is `liris/kuromi`, not `falcon`

## Boundary

The bundled smoke report in the same packet is internally behind on the ADB/reverse subsection, so treat that subsection as stale until re-proved in a fresh single-source packet.

## Use Rule

Do not keep using the older Rayssa-host packet that says `/api/mobile/bootstrap` is `404`.

For current Rayssa-host route-contract truth, treat the mobile compat lane as live and host-scoped under `liris/kuromi`.
