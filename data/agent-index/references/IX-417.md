---
ix: IX-417
name: QDD Queue local worker slot restored for local re-proof
type: reference
status: active
priority: high
tags: [qdd, queue, reproof, local, worker, mongo, env, routing, validation]
chain: [IX-416, IX-415, IX-414, IX-413, IX-412]
agents: [gaia, helm, liris, all]
---

The Queue re-proof lane is now executable in a stronger local form.

Change made:

- `D:\projects\QDD\ebacmap-master\apps\queue-server\src\index.ts`
- when `APP_ENV=local`, the queue server now boots a true `local` worker if `MONGODB_URI` is present
- it still boots the `staging` worker when `STG_MONGODB_URI` is present

Why it matters:

- the web app already queues localhost requests into `env=local`
- the repo already preserves a fully local seed lane around `mongodb://localhost:27017/ebacmap-local-test`
- restoring the local queue worker removes the biggest env-slot mismatch blocking Queue re-proof

Validation status:

- focused diff confirms the local worker-slot addition
- `pnpm --filter queue-server type-check` does not currently pass, but the failure is from missing type definition files for `minimatch` and `yup`, not from the local-worker patch itself

Practical next step:

- reconstruct `.env.local` with a local `MONGODB_URI`
- seed `ebacmap-local-test`
- configure one FTP import location
- run the bounded Queue Tier 1 smoke
