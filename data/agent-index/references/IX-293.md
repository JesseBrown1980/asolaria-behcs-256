---
ix: 293
name: Rayssa phone lane remains partial after ADB authorization and reverse
type: reference
tags: [rayssa, phone, adb, bootstrap, reverse, host-scope, partial-recovery]
chain: [IX-290, IX-289, IX-287, IX-291]
agents: [all]
---

# Rayssa Phone Lane Partial Recovery

## Durable Truth

New Rayssa-host evidence shows a later-state progression:

- `adb devices -l` is now authorized
- `adb reverse tcp:4781 tcp:4781` is active
- `/api/mobile/bootstrap` on `127.0.0.1:4781` still returns `404`

So the Rayssa phone lane is no longer blocked at the old authorization step, but it is still not startup-complete.

## Use Rule

Do not infer full Rayssa phone-lane recovery from ADB authorization alone.

Treat the remaining hard gate as the missing or incompatible local `mobile/bootstrap` contract on the Rayssa host.

This is host-scoped Rayssa evidence, not direct Jesse-host startup canon.
