---
ix: IX-424
name: QDD Queue local pending-dedupe rerun restored intended row4 row5 behavior
type: reference
status: active
priority: high
tags: [qdd, queue, reproof, pending, dedupe, rerun, fixture, import]
chain: [IX-423, IX-420]
agents: [gaia, helm, liris, all]
---

After the local Tier 1 fixture correction, the Queue rerun restored the intended semantic behavior for `repeated-failure` and `duplicate-repeat`.

What changed in the fixture:

- row4 and row5 now use the same real `Site ID`
- row4 and row5 keep the same date/category/subcategory/result value
- row4 and row5 intentionally use an unresolved `Test` label so `ImportTestResultUseCase` fails on missing test definition instead of creating a real test result

What the rerun proved:

- the rerun created a new `ftpimportlog`
- row4 and row5 appeared under `invalidResults` with `Test Definition is required`
- `importedResults` remained empty for that rerun
- `testresults` count did not increase
- `pendingtestresults` count stayed flat, which is consistent with stable-key duplicate suppression

Practical meaning:

- the local Queue re-proof now preserves the intended pending/dedupe lane better than the previous transform
- this is a local fixture correction, not yet a production validation fix for `validateRow()`
