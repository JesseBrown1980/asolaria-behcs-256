---
ix: 271
name: Local SwarmDesk source exposes direct authenticated task and MCP surfaces; screenshot scraping is unnecessary
type: reference
tags: [swarmdesk, madnessinteractive, local-source, mcp, tasks, direct-data, mqtt, dashboard, no-scraping]
chain: [IX-173, IX-171, IX-046, IX-268]
agents: [all]
---

# SwarmDesk Direct Data Plane

## Durable Truth

Dan's local SwarmDesk source exists on this machine at:

- `C:\Users\acer\swarmdesk-src\swarmdesk-dashboard-hooks.js`

That source already exposes the direct control surfaces we need:

- authenticated `/api/ai-provider-keys`
- authenticated `/api/ai/chat-completion`
- authenticated `/api/mcp`
- JSON-RPC tool invocation through `omnispindle__<function>`
- task/project tool vocabulary embedded in the hook prompt and MCP helper

The same source also points at Asolaria's browser MQTT feed on `127.0.0.1:4781`.

## Use Rule

Use this entry when deciding between UI/image automation and direct integration.

For SwarmDesk/MadnessInteractive work, prefer the authenticated task/data plane from local source or normal network calls.

Do not waste time or GPU on screenshot scraping when the task surfaces are already visible in source.
