---
ix: 173
name: Dan's MCP endpoint is ALIVE (401 auth), deeply integrated into Asolaria
type: reference
tags: [dan, mcp, madnessinteractive, omnispindle, augment-context, integration, CODE-VERIFIED]
chain: [IX-172, IX-171, IX-035]
agents: [all]
---

# Dan's MCP — Alive, Authed, Deeply Integrated

## The Endpoint
`https://madnessinteractive.cc/api/mcp/` — returns 401 Unauthorized. EXISTS and requires auth. The SwarmDesk dashboard is at `/dashboard`.

## How Deep the Integration Goes [CODE-VERIFIED from server.js + profiles]
- server.js line 579: parses `mcpServers.omnispindle` config block
- server.js line 754/780: provider="madnessinteractive" registered in system
- server.js line 994: "augment_context" listed as integration
- server.js line 11739: swarmDeskUrl points to madnessinteractive.cc/dashboard
- server.js line 23753-54: madnessinteractive.cc in CORS allowlist
- Helm profile: references augment_context in capabilities, sidecar features, and API endpoints
- Sentinel profile: same augment_context references
- Settings: superSwarmParticipants includes "augment_context"

## What augment_context IS
It's the Asolaria-side integration with Dan's MCP at madnessinteractive.cc. When super swarm dispatches work, "augment_context" is one of the 5 participants — meaning Dan's server receives tasks FROM Asolaria's swarm.

## The Omnispindle Connection
- server.js reads [mcp_servers.omnispindle] config block
- This is how Asolaria knows about Dan's Omnispindle
- The local omnispindle.js (537 lines) is a SEPARATE local spawner
- Dan's MCP is the REMOTE coordinator; local omnispindle is the LOCAL spawner

## Answer to Jesse's Question (IX-172)
The evidence points to Answer C (Both):
- Agents used Dan's MCP (augment_context) as a PARTICIPANT in super swarm
- Agents ALSO built a local omnispindle.js for local spawning
- On March 15, both may have been active simultaneously
- Dan SAW Asolaria's tasks in his SwarmDesk because the integration pushed them there
- Asolaria was partly built using another colony's infrastructure [INFERRED from code evidence]
