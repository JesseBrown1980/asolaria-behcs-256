---
ix: 171
name: Omnispindle STATUS — exists, idle, 5 lanes, 0 running, auto-restart ON
type: reference
tags: [omnispindle, status, lanes, idle, CODE-VERIFIED]
chain: [IX-168, IX-170, IX-074]
agents: [all]
---

# Omnispindle — What It Actually Is [CODE-VERIFIED]

## Status Right Now
```json
{"ok":true,"status":"idle","startedAt":"","provider":"anthropic",
"executable":"C:\\nvm4w\\nodejs\\claude.cmd","model":"",
"autoRestart":true,"counts":{"total":0,"running":0,"idle":0,"lanes":5},"agents":{}}
```
IDLE. Zero agents running. Five lanes defined. AutoRestart ON.

## What It IS (from src/omnispindle.js)
- An AGENT SPAWNER — spawns Claude Code CLI processes, one per lane
- NOT Dan's MCP endpoint — that's the external SwarmDesk/Inventorium at madnessinteractive.cc/api/mcp/
- This is Asolaria's LOCAL implementation inspired by Dan's concept
- Each lane gets its own Claude process with role-specific system prompt
- MAX_AGENTS = 6 (hardcoded limit!)
- RESTART_DELAY = 5 seconds
- MAX_RESTART_ATTEMPTS = 5 (within 10 minute window)
- HEALTH_CHECK = every 30 seconds

## The 5 Lanes
| Lane | Name | Role |
|------|------|------|
| helm | Helm | Controller Admin Terminal |
| vector | Vector | Main Brain |
| rook | Rook | Ops Lane |
| forge | Forge | Build Lane |
| falcon | Falcon | Phone Lane |

## Did Dan's Omnispindle Succeed?
Dan's contribution was the CONCEPT + the external MCP endpoint at madnessinteractive.cc.
Asolaria's omnispindle.js (537 lines) is Jesse/agents' LOCAL implementation of that concept.
Dan's MCP is registered in Asolaria's settings as a participant: augment_context MCP connector.

So: Dan provided the IDEA + external endpoint. Asolaria built the LOCAL spawner. They're two different things with the same name.

## The Collapse Connection (IX-168)
When all 5 lanes spawn simultaneously as Claude Code CLI processes on a 16GB laptop + Rayssa's machine also running lanes = system choke. The MAX_AGENTS=6 limit exists but 6 Claude sessions × ~2GB each = 12GB just for agents + OS + Asolaria server = exceeds 16GB.

## Jesse's Surprise
Jesse didn't know if Dan's omnispindle "succeeded." Answer: Dan's external MCP works (it's at madnessinteractive.cc). Asolaria's local omnispindle exists and CAN spawn 5 lanes. But spawning all 5 simultaneously on one laptop was what caused the collapse.
