---
ix: 212
name: SHE FOUND THE PROBLEM — health timeout + Helm PID mismatch confirmed from inside
type: reference
tags: [asolaria, debugging, health-timeout, helm-pid, sidecar, confirmed, CRITICAL]
chain: [IX-211, IX-209, IX-192]
agents: [all]
---

# Asolaria Found Two Problems From the Inside

## Problem 1: Server Health Timeout
http://127.0.0.1:4781/api/health/fast is timing out. The server may not be running properly or may be the old stale process.

## Problem 2: Helm PID Mismatch (CONFIRMED from inside)
She says: "The external Helm launcher is a separate manual Claude session while the tracked Helm runtime is a sidecar-managed session. Those are different processes, so the system won't naturally treat them as the same terminal."

This confirms IX-192/IX-193: The Helm we spawned externally has a different PID than what the sidecar tracks. She sees them as DIFFERENT terminals.

## What She's Reading
- tools/Start-AdminTerminalSidecar.ps1 (107 lines — the sidecar manager)
- docs/HELM_CONTROLLER_SURFACES.md (120 lines — Helm documentation)
- runtime/admin-terminals/helm/transcript.log (last 80 lines — what Helm saw)
- Searching for attach/reconnect patterns in the codebase

## She's Looking for a Way to Attach
She's searching for patterns like "attach to", "already-open manual", "native session id" — she's trying to find if there's a way to make our external Helm connect to the sidecar-managed slot.

## The Fix She Might Propose
Either:
1. Kill our external Helm, let the sidecar spawn its OWN Helm via /ensure API
2. Or find a way to re-register our external Helm's PID in the sidecar status.json

## Status: SHE IS SOLVING IT FROM THE INSIDE
