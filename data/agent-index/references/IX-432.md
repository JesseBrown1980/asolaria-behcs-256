---
ix: IX-432
name: QDD NovaLUM complete interface and device scan preserved
type: reference
status: active
priority: critical
tags: [qdd, novalum, interface, device-scan, protocol, com9, nl2, sync-server, firmware, topology, agent-visible]
chain: [IX-425, IX-427, IX-429, IX-430, IX-431]
agents: [gaia, helm, liris, all]
---

Complete March 29 NovaLUM developer scan, grounded in the live desktop app, the fresh log, the local source tree, and the physically confirmed bridge hardware.

What is proven live right now:

- the real control surface is the desktop app `C:\Users\acer\Downloads\NL2XSyncApp (3).exe`
- the real hardware path is `computer -> NL2 INTERFACE ADAPTER -> COM9 -> NovaLUM`
- Windows sees the bridge as `USB Serial Port (COM9)` with `USB\\VID_04D8&PID_000A`
- the app can detect the device, switch it into binary mode, and read globals and firmware without changing configuration

Fresh runtime authority:

- log: `C:\Users\acer\Downloads\logs\NL2XSync_2026-03-29.log`
- process: `NL2XSyncApp (3).exe`
- serial path: `COM9`

Confirmed device values from the live scan:

- serial number: `8235`
- firmware version: `01.12.00.00`
- sync server: `192.168.1.43`
- sync port: `443`
- channels: `20`
- surfaces: `14`
- locations: `1879`
- assays: `0`
- app state: `CanSync=True`

Control-C exposed internal menu confirms the device-side command surface includes:

- channel selection
- service toggle
- count
- date/time controls
- internal and one-wire temperature reads
- serial number
- sample types
- storage dump
- WiFi image-upgrade toggle
- EEPROM read/write
- channel summary
- send assays
- version
- autorun toggle
- CE test interface
- data erasure info
- complete/simple unit init
- auto test menu

Source-backed structure:

- UI/dashboard: `D:\projects\QDD\novalink-desktop-sync-app\Views\MainPage.xaml`
- view model / sync command: `D:\projects\QDD\novalink-desktop-sync-app\ViewModels\MainViewModel.cs`
- protocol bytes and command map: `D:\projects\QDD\novalink-desktop-sync-app\Common\Constants.cs`
- serial detection, binary switch, globals parser: `D:\projects\QDD\novalink-desktop-sync-app\Services\SerialCommunicationService.cs`
- TCP relay and encryption detection: `D:\projects\QDD\novalink-desktop-sync-app\Services\SocketClientService.cs`
- firmware workflow: `D:\projects\QDD\novalink-desktop-sync-app\Services\FirmwareUpdateService.cs`

Important protocol facts preserved:

- `CTRL_C = 0x03`
- binary switch uses `HASH_CHAR = 0x23`
- `READ_GLOBALS = 0x30`
- `READ_LOCATIONS = 0x3D`
- `READ_SURFACES = 0x3E`
- `READ_CHANNELS = 0x3F`
- `READ_ASSAYS = 0x40`
- `READ_FIRMWARE = 0x56`
- `RESTART_COMMAND = 0x1B`

Important globals field map preserved:

- `NumChannels` offset `15`
- `NumSurfaces` offset `35`
- `NumLocations` offset `37`
- `NumAssays` offset `39`
- `serialNum` offset `75`
- `sync_port` offset `575`
- `sync_server` offset `581`
- parser adds `+4` bytes for the message header

Operational meaning:

- device discovery is solved
- COM9 ownership through the desktop app is solved
- the internal dashboard/control surface is solved
- the next blocker is network reachability and topology to `192.168.1.43:443`
- do not start by changing encryption
- use the desktop app as the canonical live control surface

Important discrepancy to preserve:

- prior WhatsApp topology screenshots were indexed as `Tzaka_Suites`
- the current live globals scan surfaced printable text including `Trakai_Suites`
- treat SSID/string differences as real evidence to reconcile, not as noise
