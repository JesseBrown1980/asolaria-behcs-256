---
ix: 218
name: Stability checkpoint — runtime down, Helm drifted, Kuromi constrained
type: reference
tags: [stability, runtime, helm, kuromi, liris, safety, checkpoint]
chain: [IX-217, IX-170, IX-181, IX-012]
agents: [all]
---

# Stability Checkpoint — 2026-03-19

## Current State

- Main Asolaria API on `127.0.0.1:4781` is down.
- `Helm` is still alive in a sidecar shell, but the live lane is drifted onto `claude` with `--dangerously-skip-permissions` instead of the intended `codex` profile lane.
- `Gaia` and `Liris` are running on managed `codex` sidecars.
- `Kuromi` is now constrained as a lower-permission companion under `Liris`, not the primary AI identity.

## Security Note

- `tools/gaia-liris-bridge.js` exposes an unauthenticated direct bridge on `0.0.0.0:4799`.
- Treat that bridge as a suspect side channel until audited.
- Do not copy full raw language/authority packets into public memory surfaces while that path is unresolved.

## Safe Order

1. Restore the main runtime on `4781`.
2. Snapshot Helm transcript/context before replacing anything.
3. Decide whether Helm should stay on the live Claude lane for archaeology or be moved back to the intended Codex lane.
4. Keep Kuromi subordinate until bridge audit and sender-policy review are complete.
