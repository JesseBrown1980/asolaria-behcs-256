---
ix: 068
name: Launcher files inventory — what already exists
type: reference
tags: [launchers, cmd, startup, boot, inventory, FOUNDATIONAL]
chain: [IX-064, IX-065, IX-066]
agents: [all]
---

# Launcher Files Inventory

The system was ALREADY BUILT. These .cmd files exist at C:\Users\acer\Asolaria\:

## The One-Button Start (already exists!)
| File | What It Does |
|------|-------------|
| **Start-Asolaria-OneButton.cmd** | THE one-button start. Sets security env vars, calls tools\Start-Asolaria-OneButton.ps1. Skips public tunnel + Oli. |
| **Start-Asolaria.cmd** | Full start with optional schtasks, desktop auth, then calls OneButton.ps1 |
| Start-Asolaria-Core.cmd | Minimal — just calls tools\Start-Asolaria.ps1 |
| Start-Asolaria-ControlPlane.cmd | Just the control plane: tools\Start-Asolaria-ControlPlane.ps1 |

## Agent Launchers
| File | What It Does |
|------|-------------|
| **Start-Gaia.cmd** / **Gaia.cmd** | Title "Gaia — External Bridge", cd Asolaria, claude --dangerously-skip-permissions |
| **Start-Helm-External.cmd** | Helm with bootstrap-prompt.txt injected via --print flag. Falls back to interactive. |
| **Start-Dasein.cmd** | Title "Dasein", cd Asolaria, claude --dangerously-skip-permissions |
| **Start-Omnispindle.cmd** | Starts Dasein in new window + calls /api/omnispindle/start via curl |

## Phone + Shutdown
| File | What It Does |
|------|-------------|
| Start-Asolaria-Phone.cmd | Phone connector: USB+VPN+private_internet, calls tools\Start-Asolaria-Phone.ps1 |
| Stop-Asolaria.cmd | Calls tools\Stop-Asolaria.ps1 |
| Stop-Asolaria-ControlPlane.cmd | Just stops control plane |
| ADB-HealthCheck.cmd | Phone ADB health |

## The OneButton.ps1 Orchestrator
The real brain: `tools\Start-Asolaria-OneButton.ps1`
- Auto-elevates to admin if needed
- Sets security env vars (connection, network, stealth)
- Sets brain config: codex model=gpt-5.4, reasoning=xhigh, primary=codex
- Chrome email: plasmatoid@gmail.com
- Has: health guardrail, network bootstrap, tunnel keeper, tunnel monitor
- Parameters: SkipPublicTunnelKeeper, SkipTunnelMonitor, HealthGuardrailSkipOli, etc.

## Key Insight
Helm's launcher (Start-Helm-External.cmd) injects bootstrap-prompt.txt as the FIRST message. This is how Helm knows who he is on boot — the bootstrap prompt tells him to read his controller packet. THE SYSTEM WAS DESIGNED FOR THIS.

## What's Missing from This Inventory
- Start-Asolaria-SubColony.cmd (on Rayssa's machine, not here)
- MQTT broker startup (happens inside OneButton.ps1 or server.js startup)
- Admin terminal sidecar startup (managed by Asolaria server itself)
- The full OneButton.ps1 needs more reading (it's large)
