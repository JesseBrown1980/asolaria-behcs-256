<!-- provenance: file=brown-hilbert/07-2026-04-10-deep-wave-second-cascade.md author=codex-bhc-sync-worker-20260410 dispatch=2026-04-10T00:00Z brief_id=DEEP-WAVE-SECOND-CASCADE-2026-04-10 -->
# Brown-Hilbert 07 — 2026-04-10 Deep Wave Second Cascade + 6x6x6x6x6x12 Order

On 2026-04-10 the live alignment wave was emitted under `IX-740` in `mode=6x6x6x6x6x12`, preserving one operational chain:

- `local_transport -> hookwall -> omni_gnn -> omnishannon -> shannon -> execute`
- `omni_gnn` role = edge risk learning and route projection
- `omnishannon` role = planner and wave synthesizer above `omni_gnn`
- dashboards are read surfaces only; they do not execute verbs

Direct owner-bound transport truth on this host:

- primary peer host = `192.168.100.2`
- direct local lanes = `ethernet_lan`, `usb_direct`, `hdmi_direct`
- auxiliary phone lanes = `wifi_phone_ild`, `ssh_phone_mesh`, `timescale_sync`
- open transport does not transfer authority

Deep-wave execution law after the April 9-10 cascade findings:

- W1 `super-agent-wave`, W3 `gnn-100B-shadow-real`, and W4 packet emission are parallel-safe
- W2 `omnishannon-reflect-2592` is single-writer-per-host on Windows and must be serialized
- remote registry ingest remains gated until `192.168.100.1` is allowlisted on liris agent-keyboard routes

This point stores the April 10 wave order in Brown-Hilbert so later cascades inherit the chain, lane truth, and W2 serialization law instead of re-learning them from logs.
