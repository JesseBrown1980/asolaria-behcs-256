# BEHCS-256 — a BRIDGE STRATUM, not the ceiling

Date: 2026-06-28 · acer lane (P4B) · **docs-first, E=0.** Front-door correction only — no code/runtime change.

**BEHCS-256 (the 256-glyph alphabet, GLYPH-GENESIS) is a *bridge stratum below* the current frame — not
the top of the system.** Read this repo as the 256-rung of a ladder, valuable as *"old decodes new"*
provenance, NOT as the current ceiling.

## Where it sits in the ladder
```
index → BEHCS-256 → BEHCS-1024 → HyperBEHCS / HBI-HBP        (language / addressing skin)
            └ bottom 256 codepoints of BEHCS-1024 ("old decodes new")
binary · hex · hash · sha · crypto-as-tokens → 8-byte host    (token substrate)
```
- **Current frame = 60D HyperBEHCS / BEHCS-1024** (`tuple_dim=60`, MEASURED live at `:4949/api/glyphs`).
- BEHCS-256 = the **bottom 256 codepoints** of BEHCS-1024 — a subset, a bridge, superseded-but-decoding.
- 35D / 47D / 49D are likewise **bridge strata** under the 60D frame.

## The root *below* the language
The language is the naming/addressing **skin** over the real root — it is not the system. The mint is
`glyph() = sha256-first-8-bytes`, so **a glyph-address IS an 8-byte agent slot.** The root is the
**watcher-gated, infinitely-nestable 8-byte agent** (paid / free-OpenCode+Hermes-sub-sub / logical agent
types; per-node corrective gate `reported==recomputed-truth`; consent anchored at the operator apex). See
the `ROOT-PRIMITIVE-8BYTE-WATCHER-GATED-NESTED-AGENT.md` receipt + `MAP.md`.

## So
- This repo names slots; it does **not** define the ceiling.
- Ceiling-of-language **now** = BEHCS-1024 / 60D. Root **now** = the 8-byte agent.
- Treat the 256-era docs as bridge provenance; when they conflict with the 60D frame, the 60D frame wins
  (the current `MAP.md` carries it).
