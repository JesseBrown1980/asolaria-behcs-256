{
  "name": "@asolaria/kernel",
  "version": "0.1.0",
  "description": "BEHCS-256 language kernel: glyph registry, tokenizer, parser, validator, self-closure test. D11:ASSUMED until R.4 runtime grounds it.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test:closure": "node --import tsx src/closure-test.ts",
    "test:examples": "node --import tsx src/__tests__/examples.test.ts"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "behcs256": {
    "d11_level": "ASSUMED",
    "week_override": "COSIGN-MERGED-034",
    "spec": "plans/section-R-3-grammar-specification.md",
    "registry": "kernel/glyph-families.json"
  }
}
